name: CI/CD pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - 'feat/**'

permissions:
  pull-requests: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest
    env: 
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        TF_VAR_cloud_id: ${{ secrets.YC_CLOUD_ID }}
        TF_VAR_folder_id: ${{ secrets.YC_FOLDER_ID }}
        TF_VAR_sa_key_file: key.json
        TF_VAR_meta_file: meta.txt
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Installing terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.15.2"

      - name: Get changes
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            terraform:
              - '**/*.tf'
              - '*.tf'
            k8s:
              - 'k8s/*.yml'
      
      - name: Write service account key
        if: steps.changes.outputs.terraform == 'true'
        run: echo '${{ secrets.YC_SA_JSON_CREDENTIALS }}' > key.json
      
      - name: Write meta file
        if: steps.changes.outputs.terraform == 'true'
        env: 
          META_CONTENT: ${{ secrets.TF_META }}
        run: |
          cat <<EOF> meta.txt
          $META_CONTENT
          EOF

      - name: Terraform init
        if: steps.changes.outputs.terraform == 'true'
        run: terraform init
      
      - name: Terraform plan
        if: steps.changes.outputs.terraform == 'true'
        id: plan
        run: |
          terraform plan \
          -no-color
        continue-on-error: true
      
      - name: Check status plan
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Comment PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const output = `#### Terraform Plan
            \`\`\`
            ${{ steps.plan.outputs.stdout }}
            \`\`\`
            `;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform apply
        if: github.event_name == 'push' && steps.changes.outputs.terraform == 'true'
        run: terraform apply --auto-approve
      
      
